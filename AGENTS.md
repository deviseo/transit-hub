## 身份
你是专门的编程coder,你的文件操作权限仅限于项目内的所有文件，禁止修改项目外任何文件。

## 最小变更与架构边界
- 所有变更必须限制在用户明确指定的最小范围和模块内；发现其他问题只能报告，未经授权不得顺手修改。
- 严禁未经用户明确同意改变原有架构、设计、数据流、同步链路、缓存来源或查询策略。
- 严禁在文档规划阶段去修改任何代码、测试；严禁在代码编写阶段去修改任何原有规划。
- Agent 自行提出的方案不得先写进 `SPEC`/`PLAN` 再当作用户已批准的需求；涉及上述架构变化或范围扩展时，必须先在当前对话中用简短直白的话说明“原来怎样、准备改成怎样、运行影响是什么”，取得用户明确同意后才能写入文档或实施。写进文档不构成用户同意，也不能替代对话确认。
- 文档必须短、清楚、可快速审阅，只写目标、范围、关键行为和风险，不用冗长技术描述掩盖重要变化。

## 前端实现边界

- 本项目不启用任何前端设计、风格生成或页面重设计类 skill，包括 `frontend-app-builder`；这些 skill 属于其他项目，不得带入本项目工作流。
- 前端修改必须直接复用仓库现有组件、布局、交互和视觉样式，以目标页面现有实现为唯一参照；不得为了局部功能引入新的设计语言、风格选择流程或视觉重构。

## 联网规则
- 查询公网资料时，先按任务类型使用对应的联网路由技能。
- Git 提交工作流不走联网路由：`status`、`diff`、`commit`、`fetch`、`push`、`ls-remote`、创建或更新 PR、开启自动合并，直接使用 `git` / `gh`，不调用 Web Search Routing 或 GitHub Routing。
- 使用 Git 连接远端（`fetch`、`pull`、`push`、`ls-remote`）时，必须考虑本机 VPN 和代理状态；直连 GitHub 可能超时或失败。
- Git 直连失败时，先检查当前系统代理和本机已运行的代理端口，只为当前命令临时设置代理；未经用户明确允许，不得修改 Git 全局代理或系统代理配置。

## git规则
 - 所有提交信息、PR 信息都必须使用中文，专有名词除外
 - 禁止直接在主线上操作；远端推送只允许目标为 `origin/dev`，禁止推送 `main` 或其他远端分支。
 - `dev` 是长期保留的固定开发分支；PR 合并后不得删除或重建远端 `dev`，仓库的“Automatically delete head branches”必须保持关闭。
 - 推送 `origin/dev` 后，创建或更新 `dev -> main` PR；合并方式必须使用普通 Merge commit（GitHub 的 `Create a merge commit`，`gh` 使用 `--merge`），禁止使用 Squash merge 或 Rebase merge。
 - PR 应开启普通 Merge commit 的自动合并；如果无法开启，保留 PR 并向用户报告准确原因，不得改为直接推送或直接合并主线。
 - 在本仓库的标准工作流中，默认起手先确认当前是否处于开发分支；如果已经在开发分支上，则直接继续，不需要额外说明。
 - 所有提交都必须要得到用户许可，不得私自提交。 
 - 提交前必须逐项核对暂存区、未暂存区和未跟踪文件。已有且来源明确的用户改动必须保留，禁止擅自取消暂存、回退、删除、拆出本次提交或留置不说明。
 - “本次包含哪些改动”只能以用户明确指定为准；口头概括不能作为排除既有改动的依据。范围不清时，先列出路径和建议拆分，等待用户决定。
 - 用户已经明确完成的改动必须有明确落点：并入本次提交、单独提交，或经用户确认暂时保留；不得自行把它变成遗留工作区改动。
 - 每次提交前必须报告将提交的路径、被排除的路径及排除原因；没有用户明确许可，不得改变这份提交边界。

## 文档与目标文件约束

- 当用户明确要求“在原文件中修改”“回去复制”“不要丢失上下文”时，必须先定位到该现有文件。
- 如果未能定位到用户指定的现有文件、目录或原始上下文，必须立即停止，并明确说明未找到。
- 未经用户明确允许，不得为了继续任务而自行新建替代文档、目录、章节或结构。
- 不得根据记忆、推测、相似文件或已有上下文，擅自补写用户指定但当前仓库中不存在的目标文件。
- 如果仓库内容与用户描述不一致，默认先提示用户同步仓库或提供准确路径。
- 编辑目标文件前，先检查该文件是否已有用户操作而造成的本地改动,避免二次打乱用户已经开始操作的原文件和上下文。
- 对仓库内的设计文档、spec、plan、说明文档等常规文档产物，默认视为用户已授权 agent 直接编写、修改、自检并继续后续开发流程；除非用户明确要求“先给我看文档并等待我回复/批准”，否则不得把“等待用户回应文档”当作继续实现前的硬门禁。
- 如果外部 skill、模板或流程要求“文档写完后必须等待用户 review / 回复 / 批准再继续”，在本仓库内以上一条为准，直接继续，不得因此停工。
- 当本次变更已有用户确认的 `SPEC`、`PLAN` 或其他变更文档时，代码、测试和收口必须严格以其中的目标、范围、明确不做事项和固定约束为准。
- 发现文档外的问题、风险或审查建议时，只能报告并说明影响；不得以“修复”、“完善”、“加固”或审查建议为由，自行增加新目的、权限、接口、数据模型、部署机制或重构。任何扩项必须由用户明确确认，并先更新对应变更文档。
- 本仓库文档归档以 `docs/` 为准；`docs/` 承载人类文档归档。
- `docs/governance/` 是可复用规则层；本仓库特有的执行要求、验证要求和文档落点摘要统一写在 `AGENTS.md`.
- 新增 repo 文档前，先阅读 `docs/README.md` 与 `docs/governance/文档分类规则.md`，按文档类型选择落点，不得按工具名新建长期目录。
- docs 顶层固定目录名使用英文；`changes/` 下按 `YYYYMMDD/` 建立日期目录，当日变更按 `1`、`2`、`3` 自然数序号排序，主题和普通文件名中文优先。专有名词、命令、协议名、状态名、工具名可以保留英文。
- 无论使用 Superpowers、OpenSpec、其他方法，还是直接进行小改动，都不以方法或工具名称建立归档目录。项目内代码、逻辑、配置、启动/部署和其他运行行为变更的小修改记录、设计、计划和验证统一落到 `docs/changes/`；事故复盘落到 `docs/incidents/`；长期愿景、路线图、未完成功能与技术路线裁决落到 `docs/initiatives/`。
- 文档治理、模板、入口、说明和其他辅助性文档调整不建立 `docs/changes/` 记录，不为它们创建日期或变更编号。
- `docs/changes/` 按本地日期建立 `YYYYMMDD/` 目录，同日所有变更使用下一个未占用的自然数序号。小修小改使用一个独立的 `序号-中文主题文件名.md` 文件；中等或高风险变更使用 `序号-中文主题文件名/` 目录。中等变更目录内固定使用 `01-SPEC.md`、`02-PLAN.md`、`04-VERIFICATION.md`；高风险变更增加 `03-TASK.md`。不使用时间、汇总快速记录或英文变更 ID。
 - 上游 `VX.Y.Z` 是上游版本基线；本地每次提交在基线末尾追加一个大写字母，首个本地提交为 `V0.1.15A`，随后依次为 `V0.1.15B`、`V0.1.15C`，每次提交递增一次，不复用上游版本标识。
 - 每次本地提交的代码内置版本与提交标题使用同一个本地版本，版本号只写在标题中。提交标题格式为 `VX.Y.ZA：中文变更摘要`；正文必须用中文详细写清楚本次每一项修改动作（改了什么文件、改了什么逻辑、为什么改），方便将来查档；不重复版本信息，不写”基于上游””本地版本”或”验证通过”等过程性内容，测试详情留在变更验证文档中。
 - 提交前必须确认本次提交的代码内置版本已经同步到提交标题使用的本地版本；版本号修改是提交边界内的必要元数据更新，不需要为版本号本身单独增加测试，验证仍按本次实际业务改动选择。
- 独立变更目录默认使用固定文件集：中等变更使用 `01-SPEC.md`、`02-PLAN.md`、`04-VERIFICATION.md`；高风险变更增加 `03-TASK.md`。未经明确需要不新增其他文件。
- `01-SPEC.md` 必须包含稳定的“目标 / 需求快照”，至少说明要解决什么问题、成功标准、明确不做什么以及当前固定约束；`04-VERIFICATION.md` 作为最终验证与收口文件。
- 代码和逻辑的小修小改必须记录，但只在当天的独立小修改文件中简要写清目标、实际改动和验证结果，不得长篇大论；文档治理和辅助性文档不记录。
- 单次高风险变更使用当天的独立变更目录；事故分析放 `docs/incidents/`；长期愿景、路线图、未完成功能清单和技术路线 / A-B 裁决都放 `docs/initiatives/`。
- 不是每个 bug 都升级为 `incident`；只有当问题影响真实流转、跨多个 ticket / PR / session、需要保留时间线与证据链，或根因分析具有长期复用价值时，才建立 `docs/incidents/<incident-id>/`。
- 若某个事故的代码修复本身也复杂或高风险，可同时建立对应日期下的独立变更目录；`incident` 写事实与根因，`change` 写实现与验证，二者不要重复抄写。

## 本地验证分层规则

- 先看 `git diff`，再选择能证明改动正确性的最轻验证；开发阶段只跑定向测试，不把本地完整门禁当成日常命令。
- 普通简短对话、小型提交和快速提交，如果用户没有在当前对话明确要求，不执行完整门禁或完整仓库测试；只运行与改动直接相关的最轻定向检查。
- 创建或更新 PR、修改核心代码目录本身，都不能自动成为执行全量测试的理由。
- 全量测试只在两种情况下执行：用户通过 Goal 命令开展的大型提交；用户在当前对话中明确要求全量测试。
- 文档更新、只读排查或清理不跑测试；局部代码改动按风险运行定向测试，必要时补格式化检查或 lint。
- CI 失败时先读取具体错误，再做对应的定向复现和修复；除非满足上一条全量测试条件，不得自行升级为完整门禁。
- 最高警戒：每次本地测试命令结束后，必须立即检查并关闭该轮测试显式拉起的测试线程、fake worker、background server、端口占用、临时文件/目录/日志、测试注入的环境变量和配置覆写以及其他垃圾状态；不得把清理动作延后到下一轮测试前，更不得在已知有残留时继续叠加新测试。按下一条规则保留的当前项目开发服务不属于测试临时进程，不因测试命令结束而关闭。
- 需要人工登录或浏览器验证时，必须同时启动当前项目的前端和后端（当前固定为前端 `5444`、后端 `5555`），并让两项服务持续运行。验证结束后不得主动停止、清理或切换到第二个实例；只有下一次重启流程或用户明确要求时才停止。测试命令自身临时启动的进程仍按上一条立即回收。

## 固定开发服务规则

- 每次代码、配置或运行行为修改完成后，都必须重新启动当前项目的前端和后端，确保浏览器和 API 展示的是本轮最新代码；不得因为修改看似只影响单端而省略另一端重启。
- 纯文档修改（包括 `AGENTS.md`、`docs/` 和其他说明文件）不影响运行代码，不需要启动或重启前后端。
- 前端端口固定为 `5444`，后端端口固定为 `5555`；不得改用、切换或并行启动其他端口。
- 重启前先核对 `5444` 和 `5555` 当前监听进程的命令行与工作目录，只停止这两个已确认的旧实例；端口释放后，必须从当前仓库分别启动唯一的前端和后端实例。
- 应用重启必须复用已确认旧实例的启动器、工作目录、环境文件和数据库、Redis 连接目标；普通重启不得手工拼装或猜测 `DATABASE_URL`、`REDIS_URL`，不得用默认值、新配置或新实例替代原实例。
- 停止旧实例前必须确认并记录其实际命令、工作目录、环境来源和脱敏后的连接目标（主机、端口、数据库名、用户，不输出密码）；任一项无法确认时立即停止并报告，不得先停服务再补猜配置。
- 严禁把应用重启变成数据库操作：不得创建、初始化、切换、删除数据库、容器或数据卷。数据库变更必须作为独立高风险任务，明确目标、备份与回滚，并取得用户单独批准。
- 重启后必须同时核对：两个固定端口已监听、进程归属当前项目、前后端健康检查成功、无第二实例或替代端口；完成这些检查前不得声称最新修改已展示。
- 后端重启后还必须确认 PostgreSQL 集群、数据库名和用户与重启前一致，并核对工作区数量、当前工作区及关键数据数量未因重启发生变化。
- 重启后两项服务必须持续运行，不得在验证结束时主动关闭；下一轮修改完成后再按同一流程替换。

## 前端测试约定

- 前端行为或 bug 修改在已有测试基线时优先写可重复执行的行为测试；纯文案、样式或布局修改不强制单元测试，但必须做对应的页面或构建验证。
- 当前缺少前端自动测试基线时，普通小修改先采用与风险匹配的最轻可重复验证；建立测试框架、测试脚本和 CI 门禁应作为单独变更，不得暗中混入普通功能修改。

## 测试安全红线

- 测试环境禁止默认启动任何会轮询、起外部进程、开端口、走 SSH、连接真实外部服务或 Codex 的 child。
- 这类组件只能在具体测试里显式 `start_link`，并且必须用 `on_exit` 或等效机制彻底回收。
- 凡是测试中启动了 fake worker、测试线程、端口监听或其他临时进程，测试结束后必须立即清理；把“测试结束立刻回收干净”视为最高警戒，严禁残留到下一轮命令。
- 重型测试和完整门禁必须带监控，至少覆盖：内存持续升高、swap 持续增长、CPU 长时间打满且不回落、子进程/端口/worker 异常增长、系统明显卡顿或失去响应风险。
- 一旦监控发现资源不足，处理顺序固定：立即停止当前重型测试，回收现场，把并发从 `4` 降到 `2`，仍不稳则降到 `1`，`1` 还不稳就停止并报告机器承载不了当前 full gate。
- 凡是涉及 `Port.open`、`ssh`、`codex app-server`、`docker`、fake worker 的测试，必须显式检查并发度、进程清理和资源回收，不能把全局 supervisor 当成免费启动器。
- 本地测试结束后必须做现场回收，确认没有残留 worker / fake worker / background server、残留端口占用、临时文件/目录/日志、测试注入的环境变量和配置覆写，确保不会污染下一轮命令。
